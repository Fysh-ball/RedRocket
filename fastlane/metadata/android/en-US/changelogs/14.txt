Audit of all 78 source files, 46 findings fixed, plus optional Event Horizon alert enrichment. Database migrates v11 to v12 automatically.

Security: immutable PendingIntents, imports regenerate scenario IDs, dedup uses a full content hash.

Delivery: manual send dedupes numbers across scenarios; retries resolve the SMS provider once.

Data: deleting a scenario clears orphaned records, imports run in one transaction.

UI: developer mode behind a 7-tap unlock; captcha needs a correct match.
